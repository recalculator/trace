
.DS_Store
.jac/
.pipeline/
.pytest_cache/
*.dot
*.pyc
*.pyo
# "ignored directories are not ingested" test relies on, so only real
# Byte-compiled artifacts. NOTE: the `__pycache__`, `build` and `.venv`
# compiled output is excluded here - not those .py files.
# directories under sample_repo/ hold deliberate fixtures that the
# Trace
jac.local.toml
# Secrets. ANTHROPIC_API_KEY is read from the environment only - it is never
# read from a file by the application and must never be committed.
.env
.env.*
!.env.example
