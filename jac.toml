[project]
name = "Trace"
version = "0.1.0"
description = "Trace - autonomous software investigation on a code graph"
entry-point = "main.jac"
kind = "service"
jac-version = "==0.34.7"

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"
